# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.13.0
asyncpg==0.29.0
psycopg2-binary==2.9.9
aiosqlite==0.19.0

# Security & Encryption
cryptography>=3.4.8
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is the last release and predates bcrypt 4.1: it probes the
# backend at import time in a way bcrypt >=4.1 rejects, so an unpinned bcrypt
# (5.x today) makes every hash_password() call raise. Pin until passlib is
# replaced.
bcrypt==4.0.1
# SECURITY: 3.3.0 carries CVE-2024-33663 (CRITICAL, JWT algorithm confusion),
# CVE-2024-33664 and PYSEC-2025-185. The obvious bump to 3.4.0 is NOT a clean
# win: 3.4.0 caps pyasn1<0.5.0, which pulls pyasn1 0.4.8 and its own 4
# advisories, and the cap makes `python-jose==3.4.0 + pyasn1>=0.6.4`
# unresolvable. Trading an auth-path CVE for ASN.1 parser CVEs is a security
# call, not a CI one -- tracked separately for the security owner.
python-jose[cryptography]==3.3.0
# Secret-broker: attenuable capability handles (macaroons) for A2A delegation.
pymacaroons==0.13.0

# Web Automation & Scraping
playwright==1.40.0
beautifulsoup4==4.12.2
selenium==4.15.2
webdriver-manager==4.0.1
requests==2.31.0
lxml==4.9.3

# AI & Natural Language Processing
openai==1.3.8
anthropic==0.7.8
langchain==0.0.335
tiktoken==0.5.2

# Docker Integration
docker==6.1.3

# Data Processing
pandas==2.1.4
numpy==1.25.2
pydantic==2.5.0
pydantic-settings==2.1.0

# Email Processing
aiohttp==3.9.1
email-validator==2.1.0
imapclient==2.3.1

# Image Processing & Computer Vision
Pillow==10.1.0
opencv-python==4.8.1.78

# Background Tasks & Async
asyncio-mqtt==0.16.1

# Utilities
python-dotenv==1.0.0
rich==13.7.0
loguru==0.7.2
httpx==0.25.2
aiofiles==23.2.1

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.12.0
mypy==1.7.1 